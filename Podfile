# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MLPytorch' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
#  pod 'LibTorch', '~>1.5.0', :modular_headers => true
  # Pods for MLPytorch

  target 'MLPytorchTests' do
    # Pods for testing
  end

end
